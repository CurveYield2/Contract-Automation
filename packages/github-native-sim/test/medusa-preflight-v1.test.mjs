import test from 'node:test';import assert from 'node:assert/strict';import {preflightMedusaV1} from '../src/preflight/medusa-v1.mjs';
const h='a'.repeat(64),ok={observedVersion:'1.5.1',sourceSnapshotDigest:h,expectedSourceSnapshotDigest:h,harnessBundleDigest:'b'.repeat(64),expectedHarnessBundleDigest:'b'.repeat(64),cryticCompileProbe:{status:'PASS'},hasVyper:true,mixedLanguageProbe:{status:'PASS'},propertiesRequired:true,propertyTestingEnabled:true,stopOnNoTests:true,discoveredPropertyCount:2,discoveredProperties:['property_a','property_b'],coverageProbe:{status:'PASS'},parserProbe:{pass:true,falsification:true,noTests:true,unicodePrefix:true},forkRequired:true,expectedBlockHash:'0xabc',rpcProbe:{chainId:1,blockHash:'0xabc',identityNormalized:true},tinyTargetSmoke:{terminalStatus:'COMPLETED'},normalizedEvidenceContainsRpcSecret:false};
test('actual-target Medusa readiness contract PASS',()=>assert.equal(preflightMedusaV1(ok).status,'PREFLIGHT_PASS'));
test('property disabled is historical typed failure',()=>{const r=preflightMedusaV1({...ok,propertyTestingEnabled:false});assert.equal(r.firstFailure,'MEDUSA_PROPERTY_TESTING_DISABLED');assert.equal(r.diagnostics[0].historicalSignatureId,'MEDUSA-003');});
test('real output parser incompatibility blocks campaign',()=>{const r=preflightMedusaV1({...ok,parserProbe:{pass:true,falsification:false,noTests:true,unicodePrefix:true}});assert.equal(r.firstFailure,'MEDUSA_TERMINAL_OUTPUT_PARSER_INCOMPATIBLE');assert.equal(r.doNotExecute,true);});