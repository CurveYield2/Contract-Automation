import test from 'node:test';import assert from 'node:assert/strict';import {preflightCompileV1} from '../src/preflight/compile-v1.mjs';
const h='a'.repeat(64),ok={sourceSnapshotDigest:h,projectRoot:'target',projectRootExists:true,buildSystem:'mixed-native',languages:[{language:'solidity',requestedVersion:'0.8.28',installedVersion:'0.8.28'},{language:'vyper',requestedVersion:'0.3.10',installedVersion:'0.3.10'}],expectedCompilerSettingsDigest:h,observedCompilerSettingsDigest:h,dependenciesReady:true,lockfile:'package-lock.json',expectedArtifacts:['A.sol:A']};
test('mixed exact compiler matrix PASS',()=>assert.equal(preflightCompileV1(ok).status,'PREFLIGHT_PASS'));
test('wrong compiler version names exact mismatch',()=>{const r=preflightCompileV1({...ok,languages:[{language:'solidity',requestedVersion:'0.8.28',installedVersion:'0.8.30'}]});assert.equal(r.firstFailure,'COMPILE_COMPILER_VERSION_MISMATCH');assert.deepEqual(r.diagnostics[0].expected,[{language:'solidity',version:'0.8.28'}]);});