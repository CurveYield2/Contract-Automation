import test from 'node:test';import assert from 'node:assert/strict';import {preflightWorkflowV1} from '../src/preflight/workflow-v1.mjs';
const ok={repository:'CurveYield2/Contract-Automation',ref:'a'.repeat(40),workflowPath:'.github/workflows/audit-controller-execution.yml',classification:'ACTIVE_PREFLIGHT_REQUIRED',event:'pull_request',triggerAllowed:true,requiredSecrets:['SIM'],availableSecretNames:['SIM'],requiredPermissions:{contents:'read'},observedPermissions:{contents:'read'},inputSchemaValid:true,expectedOutputs:['controller-evidence.json'],substantiveJobsBlockedByPreflight:true,retry:{isRetry:false}};
test('workflow targeted PASS',()=>assert.equal(preflightWorkflowV1(ok).status,'PREFLIGHT_PASS'));
test('workflow blind retry diagnostic is precise',()=>{const r=preflightWorkflowV1({...ok,retry:{isRetry:true,failureClassified:false}});assert.equal(r.firstFailure,'WORKFLOW_BLIND_RETRY_FORBIDDEN');assert.match(r.diagnostics.at(-1).remediation,/failed job|diagnostic/i);assert.equal(r.doNotExecute,true);});