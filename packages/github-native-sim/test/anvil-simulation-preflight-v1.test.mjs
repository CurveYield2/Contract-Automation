import test from 'node:test';import assert from 'node:assert/strict';import {preflightAnvilSimulationV1} from '../src/preflight/anvil-simulation-v1.mjs';
const ok={observedVersion:'1.7.1',launcher:{exists:true,kind:'NODE_PACKAGE_BIN',path:'/repo/node_modules/@foundry-rs/anvil/bin.mjs',hardfork:'cancun'},sourceBuildDigest:'a'.repeat(64),rpcProbe:{chainId:1,blockNumber:100,blockHash:'0xabc',historicalStateAvailable:true},pinnedBlockNumber:100,pinnedBlockHash:'0xabc',requestedHardfork:'cancun',targets:[{address:'0x1',requiresCode:true,codePresent:true,addressNormalized:true}],abiChecks:[{label:'deposit',status:'PASS'}],routeChecks:[],decimalChecks:[],balanceChecks:[],approvalChecks:[],roleChecks:[],impersonationReady:true,staticRehearsals:[{label:'deposit',status:'PASS'}],snapshotReady:true,revertSnapshotReady:true,stateChangingWorkflow:true,freshAuditedDeploymentReady:true};
test('representative Anvil lifecycle readiness PASS',()=>assert.equal(preflightAnvilSimulationV1(ok).status,'PREFLIGHT_PASS'));
test('wrong ABI blocks before lifecycle',()=>{const r=preflightAnvilSimulationV1({...ok,abiChecks:[{label:'deposit(uint256)',status:'FAIL',error:'selector missing'}]});assert.equal(r.firstFailure,'ABI_SIGNATURE_MISMATCH');assert.equal(r.doNotExecute,true);});
test('missing target code blocks before lifecycle',()=>assert.equal(preflightAnvilSimulationV1({...ok,targets:[{address:'0x1',codePresent:false,addressNormalized:true}]}).firstFailure,'TARGET_CODE_MISSING'));
test('snapshot grammar regression is explicit',()=>assert.equal(preflightAnvilSimulationV1({...ok,snapshotReady:false}).firstFailure,'ANVIL_SNAPSHOT_REVERT_UNSUPPORTED'));